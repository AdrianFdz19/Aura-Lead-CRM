import React from 'react'

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  // Puedes agregar aquí elementos de UI que solo aparezcan en el dashboard
  return <>{children}</>;
}
